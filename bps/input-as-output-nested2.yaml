spec_version: 2
inputs:
  in:
    type: string
  agent:
    type: agent
grains:
  input-as-output-unique:
    kind: blueprint
    spec:
      source:
        store: bps
        path: bps/input-as-output-unique.yaml
      inputs:
        - agent: '{{ .inputs.agent }}'
        - in: '{{ .params.tenant_id }}-{{ .inputs.in }}-{{ envId }}'
      outputs:
        - output1
