spec_version: 2

inputs:
  agent:
    type: agent

grains:
  no-deploy:
    kind: shell
    spec:
      agent:
        name: '{{.inputs.agent}}'
      activities:
        destroy:
          commands:
            - 'echo vido'
