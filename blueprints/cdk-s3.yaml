spec_version: 2

inputs:
  agent:
    type: agent
    
grains:
  common:
    kind: awscdk
    spec:
      source:
        store: bps
        path: cdk/python/s3
      inputs:
      - agent: '{{ .inputs.agent }}'
