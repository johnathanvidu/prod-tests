spec_version: 2

inputs:
  agent:
    type: agent

outputs: {}

grains:
  shell1:
    kind: shell
    spec:
      agent:
        name: '{{ .inputs.agent }}'
      activities:
        deploy:
          commands:
            - "echo '=== vido-smoke-test-grain ==='"
            - "echo 'Deploy validation complete.'"
        destroy:
          commands:
            - "echo 'Destroying vido-smoke-test-grain'"
            - "echo 'Destroy complete.'"

  shell2:
    kind: shell
    spec:
      agent:
        name: '{{ .inputs.agent }}'
      activities:
        deploy:
          commands:
            - "echo '=== vido-smoke-test-grain ==='"
            - "echo 'Deploy validation complete.'"
        destroy:
          commands:
            - "echo 'Destroying vido-smoke-test-grain'"
            - "echo 'Destroy complete.'"

  shell3:
    kind: shell
    spec:
      agent:
        name: '{{ .inputs.agent }}'
      activities:
        deploy:
          commands:
            - "echo '=== vido-smoke-test-grain ==='"
            - "echo 'Deploy validation complete.'"
        destroy:
          commands:
            - "echo 'Destroying vido-smoke-test-grain'"
            - "echo 'Destroy complete.'"