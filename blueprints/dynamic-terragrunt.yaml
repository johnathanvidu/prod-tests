spec_version: 2

inputs:
  path:
    type: string

grains:
  terragrunt:
    kind: terragrunt
    spec:
      source:
        store: bps
        path: '{{ .inputs.path }}'
      agent:
        name: 'demo-prod'
