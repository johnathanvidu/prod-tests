spec_version: 2

inputs:
  path:
    type: string

grains:
  terragrunt:
    kind: terragrunt
    spec:
      source:
        store: bps
        path: terragrunt/{{ .inputs.path }}/env
      agent:
        name: 'vido-vido-prod'
