spec_version: 2

outputs:
  out:
    value: '{{ .grains.io.outputs.out }}'

grains:
  io:
    kind: terraform
    spec:
      source:
        store: bps
        path: terraform/input-as-output
      agent:
        name: vido-vido-prod
      inputs:
        - in: 'in'
      outputs:
        - out
