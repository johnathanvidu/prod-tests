spec_version: 2
description: testing sandbox...

inputs:
  agent:
    type: agent
    
grains:
  setup:
    kind: shell
    spec: 
      agent:
        name: '{{ .inputs.agent }}'
      activities:
        deploy:
          commands:
            - "echo from branch..."
        destroy:
          commands:
            - "echo destroy from branch..."
