spec_version: 2
inputs:
  in:
    type: string
  agent:
    type: agent
grains:
  input-as-output-unique:
    kind: blueprint
    spec:
      source:
        store: bps
        path: blueprints/blueprint-updates/nested2-bp-update.yaml
      inputs:
        - agent: '{{ .inputs.agent }}'
        - in: '{{ .inputs.in }}'
      outputs:
        - output1
