spec_version: 2
inputs:
  in:
    type: string
  agent:
    type: agent
grains:
  nested3:
    kind: blueprint
    spec:
      source:
        store: bps
        path: blueprints/blueprint-updates/nested3-bp-update.yaml
      inputs:
        - agent: '{{ .inputs.agent }}'
        - in: '{{ .inputs.in }}'
      outputs:
        - output1
