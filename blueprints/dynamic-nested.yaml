spec_version: 2

inputs:
  path:
    type: string
  more:
    type: string
grains:
  terragrunt:
    kind: blueprint
    spec:
      source:
        store: bps
        path: blueprints/dynamic-terragrunt.yaml
      inputs:
        - path: 'terragrunt/{{ .inputs.path }}/{{ .inputs.more }}'
