spec_version: 2

inputs:
  path:
    type: string

grains:
  parent:
    kind: blueprint
    spec:
      source:
        store: bps
        path: blueprints/dynamic-nested.yaml
      inputs:
        - path: '{{ .inputs.path }}'

